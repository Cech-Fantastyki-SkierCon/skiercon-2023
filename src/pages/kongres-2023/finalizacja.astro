---
import Kongres from '@layouts/Kongres.astro'

const free = Astro.url.searchParams.get('f')
---

<Kongres title="Preakredytacja" description="Kup bilet on-line na KONgres 2023">
  <main>
    {
      free && (
        <>
          <p class="mb-4">Dziękujemy za preakredytację!</p>
          <p class="mb-4">
            Wkrótce otrzymasz od nas maila z&nbsp;potwierdzeniem i
            <b>kodem QR</b>, który należy okazać przy akredytacji,
            aby&nbsp;otrzymać identyfikator.
          </p>
        </>
      )
    }
    {
      !free && (
        <>
          <p class="mb-4">Dziękujemy za złożenie zamówienia!</p>
          <p class="mb-4">
            Jak tylko wpłata zostanie zaksięgowana, otrzymasz od nas maila
            z&nbsp;potwierdzeniem i&nbsp;<b>kodem QR</b>, który należy okazać
            przy akredytacji, aby otrzymać identyfikator.
          </p>
        </>
      )
    }
    <p class="mb-4">
      Minęło trochę czasu, a&nbsp;mail nadal nie dotarł? Sprawdź folder ze
      spamem lub skontaktuj się z&nbsp;nami poprzez
      <a href="mailto:kongres@skiercon.pl" class="accent hover:underline"
        >kongres@skiercon.pl</a
      >
    </p>
    <p class="mb-4">Do zobaczenia na KONgresie 2023!</p>

    <a href="https://www.facebook.com/kongresfantastyki/" class="btn-back">
      Powrót do strony głównej
    </a>
  </main>
</Kongres>

<style lang="scss">
  .accent {
    color: var(--kongres-3);
  }
  .btn-back {
    padding: 15px 40px;
    border: 2px solid var(--kongres-1);
    border-radius: 30px;
    margin: 40px auto;
    display: block;
    max-width: 340px;
    background: var(--kongres-0);

    color: var(--kongres-4);
    font-weight: 700;
    text-transform: uppercase;
    font-size: 16px;
    text-align: center;

    transition: all 0.2s;

    &:hover,
    &:focus {
      transform: scale(1.05);
    }
  }
</style>
